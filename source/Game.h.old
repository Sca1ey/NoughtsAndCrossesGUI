#pragma once
#include <stdio.h>
#include <iostream>
#include <limits>

class Game {

    public:
        void initBoard();
        int getPosition(int, int );
        void setPosition(int, int, int);
        bool checkWin();
        int getPlayer();
        int switchPlayer();

    private:
        int player;
        int board[3][3];
        int winLines[8][3][2];
};