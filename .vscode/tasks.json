{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: gcc build active file",
            "command": "g++ -g ${workspaceFolder}/source/*.cpp `wx-config --cxxflags --libs` -o ${workspaceFolder}/build/main",
            "args": [
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
